# HELM CyberDeck Mobile — Stage 6A
# Suspend is the supported sleep path on this Mobile Node.
# Hibernation paths remain disabled until disk-backed swap + resume
# infrastructure is deliberately designed and tested.
[Sleep]
AllowSuspend=yes
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
